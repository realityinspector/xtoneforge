---
"@stoneforge/smithy": minor
---

Add asset upload and serving API (POST /api/assets/upload, GET /api/assets/:filename) for uploading images to .stoneforge/assets/ and serving them with immutable cache headers
