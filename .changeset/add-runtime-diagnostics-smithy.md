---
"@stoneforge/smithy": minor
---

Add GET /api/health/diagnostics endpoint that returns runtime health diagnostics including rate limit status, stuck tasks, merge queue health, error rates, and agent pool utilization.
