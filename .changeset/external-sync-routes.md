---
"@stoneforge/smithy": minor
---

Add external sync trigger and status HTTP endpoints. Provides POST endpoints for push/pull/sync operations and GET endpoints for sync status and provider listing at /api/external-sync/*.
