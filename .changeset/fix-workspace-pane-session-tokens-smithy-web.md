---
"@stoneforge/smithy-web": patch
---

Fix workspace pane token display to show only current session tokens instead of aggregating all past sessions. Update `useAgentTokens` hook to accept optional `sessionId` parameter and `useProviderMetrics` to support session-specific queries.
