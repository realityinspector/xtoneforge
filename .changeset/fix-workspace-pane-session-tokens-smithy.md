---
"@stoneforge/smithy": patch
---

Fix workspace pane token display: add session-filtered metrics query via `getBySession()` method and `sessionId` API parameter. Reduce default polling interval to 5 seconds for faster realtime updates.
