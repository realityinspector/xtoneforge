---
"@stoneforge/quarry": minor
---

Add fetch-based Linear GraphQL API client and response types for issue operations. Supports API key authentication, rate limit handling with warnings, cursor pagination, partial GraphQL error handling, and typed error responses.
