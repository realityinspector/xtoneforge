---
"@stoneforge/quarry": minor
---

Add sync engine for external service synchronization. Provides push(), pull(), and sync() operations to coordinate bidirectional sync between Stoneforge elements and external services (GitHub, Linear, etc.) with content hash change detection, configurable conflict resolution, and dry-run support.
