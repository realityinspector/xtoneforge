---
"@stoneforge/smithy": patch
---

Update built-in role definition prompts to use `sf docs dir` command instead of the two-step `sf document search` + `sf document show` pattern. Add "Getting Up to Speed" sections to worker, persistent-worker, and steward prompts.
