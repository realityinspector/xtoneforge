---
title: "Parallel Feature Development"
description: "Run multiple feature branches simultaneously with AI agents that build, test, and merge independently."
problem: "Feature development is inherently sequential. One developer works on one branch at a time, and context-switching between features kills productivity. Even with a large team, coordination overhead limits how many features ship per sprint."
icon: "parallel"
order: 1
docsLinks:
  - title: "Quick Start Guide"
    url: "https://docs.stoneforge.ai/getting-started/quick-start/"
  - title: "Task Dependencies"
    url: "https://docs.stoneforge.ai/core-concepts/dependencies/"
  - title: "Agent Roles & Orchestration"
    url: "https://docs.stoneforge.ai/core-concepts/agents/"
faqItems:
  - question: "How many features can Stoneforge develop in parallel?"
    answer: "Stoneforge can orchestrate as many parallel agents as your infrastructure supports. Teams commonly run 4-8 agents simultaneously, each working on a separate feature branch. The limiting factor is typically merge conflict complexity, not agent count."
  - question: "How does Stoneforge handle merge conflicts between parallel branches?"
    answer: "Stoneforge's Steward agents automatically detect and resolve merge conflicts. When conflicts arise, the Steward analyzes both changes, resolves straightforward conflicts automatically, and flags complex ones for human review. The dependency graph ensures conflicting tasks are sequenced when possible."
  - question: "Can I control which features are developed first?"
    answer: "Yes. Stoneforge supports task priorities, dependency chains, and plans. You can set explicit ordering, mark tasks as blocked by others, or let the Director agent optimize the execution order based on your dependency graph."
  - question: "Does parallel feature development work with monorepos?"
    answer: "Absolutely. Stoneforge uses isolated git worktrees for each agent, so parallel work across different packages in a monorepo is conflict-free by default. The dependency system understands cross-package relationships."
  - question: "What happens if one feature branch breaks the build?"
    answer: "Each agent works in its own worktree with independent builds. A broken build in one branch doesn't affect others. The Steward agent runs CI checks before merging and will hold broken branches until they pass."
---

Stoneforge turns sequential feature development into a parallel operation. Instead of one developer working on one branch, you describe the features you need and let the orchestrator dispatch them across multiple AI agents simultaneously.

### Isolated worktrees, zero interference

Each agent gets its own [git worktree](https://docs.stoneforge.ai/core-concepts/worktrees/) — a fully isolated copy of your repository. Agent-1 can be building a new API endpoint while Agent-2 rewrites the dashboard component and Agent-3 adds email notifications. They never step on each other's code.

```bash
# Create a plan with multiple features
sf plan create --title "Sprint 14 Features"

# Add feature tasks — they'll be dispatched in parallel
sf task create --title "Add user invitation flow" --plan "Sprint 14 Features"
sf task create --title "Build analytics dashboard" --plan "Sprint 14 Features"
sf task create --title "Implement webhook retry logic" --plan "Sprint 14 Features"

# Start orchestration — agents pick up tasks automatically
sf serve
```

### Dependency-aware dispatch

Not all features are independent. Stoneforge's [dependency system](https://docs.stoneforge.ai/core-concepts/dependencies/) lets you express relationships between tasks. If the analytics dashboard depends on a new API endpoint, the orchestrator waits for the endpoint task to complete before starting the dashboard.

```bash
# The dashboard depends on the API endpoint being ready
sf task create --title "Build analytics API endpoint" --plan "Sprint 14 Features"
sf task create --title "Build analytics dashboard" --plan "Sprint 14 Features" \
  --depends-on "Build analytics API endpoint"
```

### Automatic merge and review

When an agent completes a feature, the Steward agent takes over — running tests, checking for conflicts, and merging the branch. You get a merge request with a summary of changes, test results, and any issues flagged during review.

### Real-time visibility

The Stoneforge [web dashboard](https://docs.stoneforge.ai/getting-started/dashboard/) shows every agent's progress in real time. See which features are in progress, which are in review, and which have merged — all from a single view.
