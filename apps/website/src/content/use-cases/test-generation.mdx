---
title: "Test Generation at Scale"
description: "Generate comprehensive test suites across your codebase with parallel AI agents that understand your code patterns."
problem: "Test coverage is perpetually behind. Writing tests is time-consuming, and developers prioritize shipping features over writing tests. The result: untested code accumulates, regressions slip through, and the team loses confidence in deployments."
icon: "test"
order: 4
docsLinks:
  - title: "Quick Start Guide"
    url: "https://docs.stoneforge.ai/getting-started/quick-start/"
  - title: "Plans & Task Breakdown"
    url: "https://docs.stoneforge.ai/core-concepts/plans/"
  - title: "Workspace Configuration"
    url: "https://docs.stoneforge.ai/guides/configuration/"
faqItems:
  - question: "What types of tests can Stoneforge agents generate?"
    answer: "Agents can generate unit tests, integration tests, and end-to-end tests. They analyze your existing test patterns (framework, assertion style, mocking approach) and generate tests that match your conventions. Supported frameworks include Jest, Vitest, Pytest, Go testing, and more."
  - question: "How does Stoneforge ensure generated tests are meaningful?"
    answer: "Agents don't just generate tests that pass — they analyze code paths, edge cases, and error handling to create tests that catch real bugs. Each agent runs the test suite to verify tests pass, and the Steward reviews test quality before merging."
  - question: "Can I target specific directories or modules for test generation?"
    answer: "Yes. You can scope test generation tasks to specific directories, file patterns, or modules. For example, generate tests only for the /api/routes/ directory, or target all files with less than 50% coverage."
  - question: "Will generated tests conflict with my existing test suite?"
    answer: "No. Agents analyze your existing tests before generating new ones to avoid duplication. They follow your naming conventions, use the same test utilities, and organize tests in the same directory structure you already use."
  - question: "How much coverage improvement can I expect?"
    answer: "Teams typically see a 30-60% coverage improvement in the first run, depending on starting coverage. Agents focus on untested code paths, edge cases, and error handlers — the areas most likely to harbor bugs."
---

Stoneforge makes it practical to generate a comprehensive test suite across your entire codebase. Instead of one developer writing tests file by file, multiple agents work in parallel — each targeting different modules.

### Coverage audit, then parallel generation

Start by identifying the gaps. Stoneforge agents can scan your codebase, identify untested modules, and create a plan to cover them systematically.

```bash
# Create a test generation plan
sf plan create --title "Increase test coverage to 80%" \
  --description "Generate unit and integration tests for all modules below 50% coverage. Follow existing test patterns in __tests__/ directories."

# The Director creates scoped tasks:
# 1. Generate tests for src/services/auth/ (12% coverage)
# 2. Generate tests for src/services/billing/ (8% coverage)
# 3. Generate tests for src/api/routes/ (34% coverage)
# 4. Generate tests for src/utils/ (45% coverage)
# 5. Generate integration tests for API endpoints
```

### Pattern-aware test generation

Agents study your existing tests before writing new ones. They learn your [testing conventions](https://docs.stoneforge.ai/guides/configuration/) — which framework you use, how you structure test files, what mocking patterns you prefer — and generate tests that fit in seamlessly.

```bash
# Configure workspace-level testing conventions
sf init

# In .stoneforge/prompts/worker.md, add:
# "Follow existing test patterns. Use Vitest with vi.mock().
#  Place test files adjacent to source files as *.test.ts.
#  Use factory functions for test data, not raw objects."
```

### Every test runs before merge

Agents don't just write tests — they run them. Each generated test suite is executed in the agent's worktree to verify everything passes. The Steward agent runs the full test suite one more time before merging to catch any interactions between newly generated tests.

### Target the riskiest code first

Focus test generation where it matters most. Prioritize modules that handle payments, authentication, data integrity — the code where a bug would be most damaging. Stoneforge's priority system ensures high-risk modules get tested first.

```bash
# High-priority: test the critical paths first
sf task create --title "Generate tests for payment processing" --priority 1
sf task create --title "Generate tests for auth middleware" --priority 1
sf task create --title "Generate tests for data export" --priority 2
sf task create --title "Generate tests for UI components" --priority 3
```
